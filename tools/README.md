# Tools

Python scripts that do the actual deterministic work: API calls, data transformations,
file operations, database queries. Before writing a new script, check here first —
only add one when nothing existing covers the task. See `../docs/PRD.md` for the full
WAT architecture this convention is part of.

This directory is currently empty — tools will be added once the QR-Trends product
scope is defined.
